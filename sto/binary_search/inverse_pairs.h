#ifndef INVERSE_PAIRS_H
#define INVERSE_PAIRS_H
#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>

/**
 * 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可
 *
 * 
 * @param nums int整型一维数组 
 * @param numsLen int nums数组长度
 * @return int整型
 */
int InversePairs(int* nums, int numsLen );

#endif